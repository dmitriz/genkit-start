{
  "name": "genkit",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "devtool": "npx genkit start -- node index.js & (while ! curl -s http://localhost:4000 > /dev/null; do sleep 0.5; done && sensible-browser http://localhost:4000)",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@genkit-ai/googleai": "^1.6.0",
    "dotenv": "^16.5.0",
    "genkit": "^1.6.0"
  },
  "devDependencies": {
    "genkit-cli": "^1.6.1"
  }
}
